nob = function nob(fname,lname,middle) {
this.fname = fname;
this.middle = middle;
this.lname = lname;
fullN = fname +" "+ middle + " " + lname; 
return fullN;
}

userName = nob('MyFirst','MyLast','Middle');
//make user input pass through variable. Change to something like: 
userName = nob(prompt());
//also add other id and cookie-number = " "; and member-id = " ";nob = function nob(fname,lname,middle) {
this.fname = fname;
this.middle = middle;
this.lname = lname;
fullN = fname +" "+ middle + " " + lname; 
return fullN;
}

userName = nob('MyFirst','MyLast','Middle');
//make user input pass through variable. Change to something like: 
userName = nob(prompt());
//also add other id and cookie-number = " "; and member-id = " ";